
///<reference path='../typings/tsd.d.ts' />

// import * as action from './action'

describe('action', () => {
    // describe('constructor given a `params` object', () => {
    //     it('should clone properties on the params object whose names match own properties', () => {

    //         let params = {
    //             id: 'fb/29j9dj2sldfjk',
    //             email: 'bryn.bellomy@gmail.com',
    //             extraEmail: 'bryn@listenonrepeat.com',
    //             fbUser: { first_name: 'bryn' },
    //             hearts: 89,
    //             repeats: 156
    //         }

    //         let user = new User(params)

    //         expect(user.id).toEqual(params.id)
    //         expect(user.email).toEqual(params.email)
    //         expect(user.extraEmail).toEqual(params.extraEmail)
    //         expect(user.fbUser).toEqual(params.fbUser)
    //         expect(user.hearts).toEqual(params.hearts)
    //         expect(user.repeats).toEqual(params.repeats)
    //     })
    // })
})